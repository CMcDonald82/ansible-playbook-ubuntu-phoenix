---

# Place sensitive variables (such as passwords or secrets) specific to the users role here
# Encrypt this file with Ansible Vault when finished:
# ansible-vault encrypt environments/production/group_vars/all/users/vault.yml

vault_deploy_username: deploy
vault_deploy_password: vault_deploy_password # Replace this with a password generated using passlib (see README for details)